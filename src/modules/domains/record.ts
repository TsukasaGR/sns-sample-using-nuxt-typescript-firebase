// TODO: ロジックを実装する
// import * as types from '~/types/domainTypes'
// import { dayjsFromFirestoreTimestamp, dayjsFromNow } from '~/modules/dayjs'

// export function isCompleted(record: types.Record): boolean {
//   return record.completedAt !== undefined && record.completedAt !== null
// }

// export function isExpired(record: types.Record): boolean {
//   return dayjsFromFirestoreTimestamp(record.recordAt).diff(dayjsFromNow()) < 0 && !isCompleted(record)
// }
